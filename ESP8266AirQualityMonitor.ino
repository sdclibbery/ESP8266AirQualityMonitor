#define ANALOGUE_IN_PIN A0
#define LED_OUT_PIN D0

void setup() {
  pinMode(ANALOGUE_IN_PIN, INPUT);
  pinMode(LED_OUT_PIN, OUTPUT);
}

void loop() {
//  digitalWrite(LED_BUILTIN, HIGH);
//  delay(1000);
}
